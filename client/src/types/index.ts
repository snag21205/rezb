// Client-side types
// Shared types are imported from @shared/types/*

export interface NavItem {
  label: string
  href: string
  icon: string
}

export interface PageMeta {
  title: string
  description?: string
}
